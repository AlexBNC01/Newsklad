# Тестирование без БД  
NODE_ENV=development
PORT=3000
SKIP_DB_SETUP=true
DATABASE_URL=sqlite:./test.db

# JWT
JWT_SECRET=test-secret-key
JWT_REFRESH_SECRET=test-refresh-secret
JWT_EXPIRE_TIME=15m
JWT_REFRESH_EXPIRE_TIME=7d

# Отключаем БД для тестов API
DATABASE_URL=sqlite::memory:
DB_SSL=false